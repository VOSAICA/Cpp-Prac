#include <iostream>
#include <list>
#include <map>
#include <vector>

using std::cout;
using std::list;
using std::map;
using std::vector;

int main()
{
    map<vector<int>::iterator, int> m1;

    map<list<int>::iterator, int> m2;

    return 0;
}

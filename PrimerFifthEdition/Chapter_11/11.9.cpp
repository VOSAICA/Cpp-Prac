#include <iostream>
#include <list>
#include <map>
#include <string>

using std::cout;
using std::list;
using std::map;
using std::string;

int main()
{
    map<string, list<size_t>> m;

    return 0;
}
